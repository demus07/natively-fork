[package]
name = "native-audio"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2", features = ["async"] }
napi-derive = "2"
cpal = "0.15"
once_cell = "1.20"

[build-dependencies]
napi-build = "2"
